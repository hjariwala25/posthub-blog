const Message = ({ onGetPostClick }) => {
    return (
        <center className="message">
            <h1 className="post-bottom">There are no Posts !!</h1>
            
        </center>
    );
}

export default Message;
